# Issues/changes reported during testing 2026-07-27
- The latest code list jsonld should be automatically copied if this is not already the case
- `dct:temporal` as relevantDataType indicates a date range (start/end)
- The page margins/padding is not correct and all text/elements are near the edges
- `xsd:duration` as relevantDataType indicates a duration (dd:hh:mm:ss)
- Added `conditionPath` (shacl path of a condition) and `conditionValue` (shacl hasvalue of a condition) that indicates that a concept is only shown when the concept at the path is a specific value
